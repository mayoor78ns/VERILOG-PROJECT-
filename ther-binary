module Thermo_binary(

    input [6:0] i,

    output [2:0] b

    );

    wire s1, s2, s3, s4, s5;

    FA_data d1(i[5], i[4], i[3], s1, s2);

    FA_data d2(i[2], i[1], i[0], s3, s4);

    FA_data d3(i[6], s1, s3, b[0], s5);

    FA_data d4(s5, s2, s4, b[1], b[2]);

endmodule
